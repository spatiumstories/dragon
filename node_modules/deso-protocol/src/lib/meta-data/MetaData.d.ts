import { Identity } from '../identity/Identity';
import { Node } from '../Node/Node';
import { GetAppStateRequest, GetAppStateResponse, GetExchangeRateResponse } from 'deso-protocol-types';
export declare class MetaData {
    private node;
    private identity;
    constructor(node: Node, identity: Identity);
    getAppState(request: GetAppStateRequest): Promise<GetAppStateResponse>;
    getExchangeRate(): Promise<GetExchangeRateResponse>;
    healthCheck(): Promise<number>;
}
